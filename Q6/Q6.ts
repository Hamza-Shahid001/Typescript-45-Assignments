// Store a person’s name, and include some whitespace characters at the beginning and end of the name.
// Print the name once, so the whitespace around the name is displayed.
// Then print the name after striping the white spaces.

let personName:string = '\t Muhammad \n Hamza Shahid\n' // Store the name in variable with whitespaces

console.log(personName) // Print the variable with whitespaces

let person_name= 'Muhammad Hamza Shahid' // Store the name in variable without whitespaces

console.log(person_name) // Print the variable without whitespaces